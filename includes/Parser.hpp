#ifndef PARSER_HPP
#define PARSER_HPP



class Parser {

	private:

		

	public:

		Parser();
		consttype(eType yy, T data)
		{
			map[yy] = &(consint)(int)
			return map.at(yy)(data);
			return consint(data)
		}

}

#endif
