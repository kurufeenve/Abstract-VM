#ifndef START_HPP
#define START_HPP

class   Start
{
    private:

    public:
        Start(void);
        Start(const Start &St);
        virtual ~Start(void);
        Start           &operator= (const Start &St);
};

#endif
